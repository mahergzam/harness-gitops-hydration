apiVersion: v1
kind: Service
metadata:
  name: preprod-test-app
  namespace: test-app-preprod
spec:
  selector:
    app: test-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
