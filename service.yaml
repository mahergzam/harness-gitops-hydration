apiVersion: v1
kind: Service
metadata:
  name: prod-test-app
  namespace: test-app-prod
spec:
  selector:
    app: test-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
